<main class="lm-inner lm-module-container">
<span id="lm-show-item">
	<span id="lm-show-count"></span>
</span>
 <div class="lm-container-fluid fade out"> 
 <div class="lm-scroll-inner">
	        <h1></h1>
	        <?php 
		        echo '<pre>' . print_r($this->session->userdata, TRUE) . '</pre>';
		        
	        ?>
	        This example is a quick exercise to illustrate how the default, static and fixed to top navbar work. It includes the responsive CSS and HTML, so it also adapts to your viewport and device.
	        To see the difference between static and fixed top navbars, just scroll.
	          <a class="btn btn-lg btn-primary" href="../../components/#navbar" role="button">View navbar docs &raquo;</a>
	</div>
	<div class="lm-scroll-to-reveal lm-col-2">
	        <h1><?php echo uri_string();?></h1>
	        <?php 
		        echo '<pre>' . print_r($this->session->userdata, TRUE) . '</pre>';
		        
	        ?>
	        This example is a quick exercise to illustrate how the default, static and fixed to top navbar work. It includes the responsive CSS and HTML, so it also adapts to your viewport and device. To see the difference between static and fixed top navbars, just scroll.
	        <a class="btn btn-lg btn-primary" href="../../components/#navbar" role="button">View navbar docs &raquo;</a>
	</div>
	<div class="lm-scroll-to-reveal lm-col-2">
	        <h1><?php echo uri_string();?></h1>
	        <?php 
		        echo '<pre>' . print_r($this->session->userdata, TRUE) . '</pre>';
		        
	        ?>
	        This example is a quick exercise to illustrate how the default, static and fixed to top navbar work. It includes the responsive CSS and HTML, so it also adapts to your viewport and device.
	        To see the difference between static and fixed top navbars, just scroll.
	        <a class="btn btn-lg btn-primary" href="../../components/#navbar" role="button">View navbar docs &raquo;</a>
	        	</div>
	
	<div class="lm-scroll-to-reveal lm-col-2">
	        <h1><?php echo uri_string();?></h1>
	        <?php 
		        echo '<pre>' . print_r($this->session->userdata, TRUE) . '</pre>';
		        
	        ?>
	        This example is a quick exercise to illustrate how the default, static and fixed to top navbar work. It includes the responsive CSS and HTML, so it also adapts to your viewport and device. To see the difference between static and fixed top navbars, just scroll.
	        <a class="btn btn-lg btn-primary" href="../../components/#navbar" role="button">View navbar docs &raquo;</a>
	</div>
	<div class="lm-scroll-to-reveal lm-col-2">
	        <h1><?php echo uri_string();?></h1>
	        <p>
	        <?php 
		        echo '<pre>' . print_r($this->session->userdata, TRUE) . '</pre>';
		        
	        ?>
	        This example is a quick exercise to illustrate how the default, static and fixed to top navbar work. It includes the responsive CSS and HTML, so it also adapts to your viewport and device.
	        To see the difference between static and fixed top navbars, just scroll.
	         <a class="btn btn-lg btn-primary" href="../../components/#navbar" role="button">View navbar docs &raquo;</a>
	</div>
	
</div>
</main>
</div><!-- Lm-Page -->
<span class='nprogress-logo fade out'></span>